# Test fixtures

These files are retained standard-JSON fixtures from the former implementation:

- `mock.json` — compact representative data;
- `mock-large.json` — larger nested data;
- `mock-edge-cases.json` — broad scalar and structural edge cases.

JSON5 fixtures were removed because version 2 accepts standard JSON only.
